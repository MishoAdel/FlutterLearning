class ExpensesItemModel {
  final String title, subTitle, money, iconImg;

  const ExpensesItemModel(
      {required this.title,
      required this.subTitle,
      required this.money,
      required this.iconImg});
}
